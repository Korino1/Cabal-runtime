[package]
name = "cabal-runtime-hook"
version = "0.7.0"
edition = "2024"
rust-version = "1.85"
description = "Native Codex context gateway with deterministic change policy enforcement"
license = "MIT OR Apache-2.0"

[dependencies]
cabal-observe = { path = "../.." }
cabal-change-policy = { path = "../cabal-change-policy" }
cabal-completion-gate = { path = "../cabal-completion-gate" }
cabal-delta = { path = "../cabal-delta" }
cabal-file-cache = { path = "../cabal-file-cache" }
cabal-log = { path = "../cabal-log" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

[dev-dependencies]
tempfile = "3"
